#!/usr/bin/python


mips_endian_prefix = 'mips64-linux-gnuabi64-'
mips_endian_prefix_qemu = 'mips64'

mips_as_flags = '-fPIC -march=mips64r2'
mips_cc_flags = '-fPIC -march=mips64r2'

mips_qemu_default_cpu = 'MIPS64R2-generic'

include(arch_dir_map['mips'] + 'tmake.inc.mips')
