#!/usr/bin/python


mips_endian_prefix = 'mipsel-linux-gnu-'
mips_endian_prefix_qemu = 'mipsel'

mips_as_flags = '-fPIC -march=mips32r2'
mips_cc_flags = '-fPIC -march=mips32r2'

mips_qemu_default_cpu = '74Kf'

include(arch_dir_map['mips'] + 'tmake.inc.mips')
